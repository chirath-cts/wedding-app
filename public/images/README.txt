Put your photos here using these exact filenames:

  hero.jpg       -> the big couple photo on the first screen (landscape works best, at least 1600px wide)
  story-1.jpg    -> first "Our Story" photo
  story-2.jpg    -> second "Our Story" photo

If you want different filenames, that's fine too — just update the matching
paths in src/lib/config.ts (heroImage and storyImages).
